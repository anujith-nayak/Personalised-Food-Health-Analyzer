# Copy this to .env and fill in your values

# When deployed (Render/Railway backend), replace with your backend URL
# The Vite proxy handles /api → backend automatically during local dev
VITE_API_URL=http://localhost:8000
